setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    release:
      dockerfile: Dockerfile
      target: prod
    web: Dockerfile
release:
  image: web
  command:
    - echo "Hello, Heroku!";
# blah
